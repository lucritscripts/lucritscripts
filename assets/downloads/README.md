Put your gated files here (templates, checklists, PDFs, zips).

Anything in this folder is publicly reachable by direct URL — the locker
gates the *link*, not the file. For true protection, host the file behind
the Worker and only hand out a signed URL after /verify succeeds.
